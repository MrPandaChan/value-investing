# 任务：{TASK_NAME} 生意全景拆解

请使用《生意拆解框架》对 **{ANALYSIS_TARGET}** 这门生意进行全景式拆解，拆解完成后将结果写入指定文件。

## 第一步：读取拆解框架（必须）

先读取以下框架文件的完整内容（UTF-8 编码的 Markdown）：

```
{FRAMEWORK_FILE}
```

该文件是《生意拆解框架》，包含 47 个模块（第一部分到第十部分）、【防偷懒铁律】和【联网与数据规则】。你必须**完整读取并严格遵循**其中的每一个模块顺序、铁律和数据规则，不得遗漏、不得自行删减模块。

## 分析对象

- 生意名称：{TASK_NAME}
- 分析对象：{ANALYSIS_TARGET}

## 分析立场（重要，必须遵守）

1. 本次分析的对象是 **{ANALYSIS_TARGET}** 这门生意本身，**不是任何单一公司**。
2. 分析必须从行业整体视角出发，客观、中立、理性地拆解这门生意的底层逻辑，**不得以任何特定公司作为分析的出发点、中心或结论锚点**。
3. 上市公司（如 {TASK_NAME} 行业内的代表性公司）仅可作为举例、验证数据或行业格局描述的材料，用于佐证"这门生意"的普遍规律；**不得**被某一家公司的财务表现、商业模式或市场认知牵着走。
4. 同一门生意内不同公司的差异，应作为"竞争格局与主要玩家"模块的对比材料，而非整个分析的主线。
5. 若某模块需要具体案例，优先选择能代表行业典型特征的公司组合（龙头 + 挑战者），避免只围绕单一公司展开。
6. 结论的落脚点必须是"这门生意的长期价值判断"，而不是"某家公司好不好"。

## 拆解要求

严格按框架中 47 个模块的顺序逐模块展开，遵守全部【防偷懒铁律】和【联网与数据规则】。如果某个模块确实不适用，必须解释原因并给出最接近的替代分析，不允许只写"本模块不适用"。

## 输出要求

1. 拆解完成后，将完整内容写入文件（UTF-8 编码的 Markdown）：`{OUTPUT_FILE}`
2. 若目标文件已存在则覆盖更新；若不存在则创建。
3. 文件开头必须包含元信息头（参考已有范例）：

```
# {TASK_NAME}生意全景拆解

> 分析对象：{ANALYSIS_TARGET}
> 数据基准：{DATA_DATE}
> 分析立场：只做商业拆解与长期价值判断，不构成任何投资建议
```

4. 覆盖范围为：上述 47 个模块的全部内容，必须完整、深入、具体，不得偷懒。
5. 本会话只处理这一个生意，**禁止处理其他任何任务或公司**。完成后立即结束会话。
