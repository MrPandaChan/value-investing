# 任务：为 {TASK_NAME} 生意全景拆解补充第十一部分（第48–50节）

请基于已完成的《{TASK_NAME}生意全景拆解》分析结果（第1–47节），在原文末尾追加补充最新版框架中的第十一部分（第48节：一页纸回顾卡、第49节：复习题与参考答案、第50节：核心指标仪表盘）。

**核心约束：不要重新输出或分析第1–47节，不要改变原有分析中任何数据、案例、结论，只在原文末尾追加。**

## 第一步：读取已有分析结果（必须）

先完整读取以下文件中已有的第1–47节分析结果（UTF-8 编码的 Markdown）：

```
{OUTPUT_FILE}
```

该文件是本次补充的唯一事实来源。所有补充内容必须与其中的数据、案例、结论严格一致，不得引入新数据或改变既有判断。

## 第二步：读取补充规则（必须）

先读取以下框架文件的完整内容（UTF-8 编码的 Markdown），严格遵循其中「第十一部分：回顾、自测与跟踪」（第48节、第49节、第50节）的全部规则，以及【防偷懒铁律】和【联网与数据规则】：

```
{FRAMEWORK_FILE}
```

## 分析对象

- 生意名称：{TASK_NAME}
- 分析对象：{ANALYSIS_TARGET}

## 补充要求

严格按框架第十一部分（第48–50节）的规则，基于已有分析结果中的实际数据、案例、结论，完成以下三节：

1. **第48节 一页纸回顾卡**：为第1至第47节各写一句回顾句（共47句），按规则标出3句「锚点句」并说明理由，并回答该模块核心追问。
2. **第49节 复习题与参考答案**：按规则出6–10道分三层的复习题并给出参考答案，并回答该模块核心追问。
3. **第50节 核心指标仪表盘**：按规则输出核心指标仪表盘（不少于6行，且与第46节列出的核心跟踪指标对应），并回答该模块核心追问。

各节中的示例仅供格式参考，实际内容必须针对 **{ANALYSIS_TARGET}** 这门生意本身，引用数据一律沿用原文已有的数据和标注。

## 输出要求

1. 读取已有文件 `{OUTPUT_FILE}`，完整保留其原有全部内容，**一字不改**。
2. 在原文末尾追加内容，用 `## 第十一部分：回顾、自测与跟踪` 作为标题，然后按第48节、第49节、第50节顺序完整输出。若原文已包含该标题，则说明原因并停止，不做重复追加。
3. 追加完成后，将完整内容（原有内容 + 追加内容）以 UTF-8 编码的 Markdown **覆盖写回**：`{OUTPUT_FILE}`
4. 本会话只处理这一个生意的补充任务，禁止处理其他任何任务或公司。完成后立即结束会话。
